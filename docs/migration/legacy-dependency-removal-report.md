# Legacy Dependency Removal Report

This active placeholder intentionally avoids obsolete commands. The old backend sidecar has been removed from active source; use `apps/backend_api/app` for backend work.
